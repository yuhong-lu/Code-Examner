server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/code_examner?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 962464
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

# JWT配置
jwt:
  secret: mySuperSecretKeyForJWTTokenGenerationThatIsAtLeast32CharactersLong
  expiration: 86400000  # 24小时 (毫秒)

logging:
  level:
    com.yourexamner: DEBUG
    org.springframework.security: DEBUG